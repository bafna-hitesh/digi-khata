const path = require('path');

module.exports = {
  // Source files
  src: './src',

  // Production build files
  build: './dist',

  // Static files that get copied to build folder
  public: './public',
};
