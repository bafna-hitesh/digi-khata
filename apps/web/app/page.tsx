'use client';
import { Layout, Header } from '@digi/components';
import Logo from '@/assets/Logo';

export default function Page() {
  return (
    <Layout>
      <Header>
        <Logo />
      </Header>
    </Layout>
  );
}
